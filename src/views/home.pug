extend base
include mixins/video

block content
    div.body__container
        include partials/menu 
        div.video-grid
            each video in videos
                +video(video)
            else
                span.empty__message No videos found