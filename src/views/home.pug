extend base
include mixins/video

block content
    div.body__container
        include partials/menu 
        div.video-grid
            each video in videos
                +video(video)
                +video(video)
                +video(video)
                +video(video)
                +video(video)
                +video(video)
                +video(video)
                +video(video)
                +video(video)
                +video(video)
            else
                span.empty__message No videos found