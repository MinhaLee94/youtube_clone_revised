extends base
include mixins/video

block content
  div.body__container
    include partials/menu 
    div.video__search-list
      each video in videos
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
      else
        span.empty__message No videos found


